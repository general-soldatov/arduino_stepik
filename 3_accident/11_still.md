**Вова**: Но как мы можем вывести текст в консоль? Ведь программа работает на плате, а не на компьютере!

**Маша**: Ну конечно же, плата может общаться с компьютером двусторонним способом! Не только получать прошивку, но и передавать компьютеру информацию. Ведь так?

**Артём**: Правильно! Общение платы Arduino и компьютера осуществляется через протокол UART. Я не буду подробно рассказывать, что это протокол, потому что в Arduino реализован специальный объект Serial, который как раз и помогает общаться компьютеру или другим устройствам с нашей платой. 

**Вова**: А что можно передавать с помощью этого протокола? Текст или числа?

**Артём**: Вообще и то, и то. А пока что я покажу, как мы можем передавать компьютеру информацию в текстовом виде. 

Для начала в функции `setup()` мы задаём скорость общения компьютера и микроконтроллера. Скорость передачи данных задаётся в бодах (boud rate - бит в секунду) и она должна совпадать на обоих устройствах:
```cpp
void setup() {
    Serial.begin(115200);
}
```

**Вова**: Я понял! Это нужно, чтобы устройства понимали друг друга. Иначе будет как в зверополисе.

<details><summary><b>Мемасик</b></summary>
<img width="1131" height="807" alt="image" src="https://github.com/user-attachments/assets/b172d534-8f7a-4437-86f2-cb637f2362c1" />

</details>


**Артём**: Не могу сказать, поскольку не видел этого фильма 😢

**Маша**: А число 115200 - это и есть те самые боды?

**Артём**: Да, боды или количество бит в секунду. Теперь в функцию loop() мы запишем:
```cpp
void loop() {
    Serial.println("Hello, world!");
    delay(1000);
}
```
Таким образом мы можем передавать текст нашему компьютеру с каждой строчки с периодичностью в 1 секунду.

Маша написала код в IDE и прошила микроконтроллер. Но никаких изменений не произошло.
