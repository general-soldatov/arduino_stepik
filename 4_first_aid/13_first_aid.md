**Маша**: У нас получилось поморгать зелёным фонарём. Его мы подключим к 4 выводу! Теперь надо попробовать использовать ШИМ.

**Вова**: Это замечательно! Теперь давай с ШИМ разбираться. Раз ты задействовала 4-й вывод, то можно взять 3-й, поскольку рядом с ним есть знак ~.

Маша начала писать код по примеру из книги.

int delay_pwm = 5, pin = 3;
void setup() {
pinMode(pin, OUTPUT);
}
void loop() {
for (int i = 0; i <= 255; i++) {
analogWrite(pin, i);
delay(delay_pwm);
}
delay(1000);
}

После успешной компиляции код заработал и светодиод начал увечивать яркость, потом снова начал загораться заново.

**Вова**: Теперь нам осталось посчитать, сколько нужно ставить задержку в переменную delay_pwm... Только я предполагаю, что целое число у нас не получится...
